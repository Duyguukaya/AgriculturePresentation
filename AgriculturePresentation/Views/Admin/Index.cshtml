@using EntityLayer.Concrete
@model List<Admin>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}


@{
    var count = 0;
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-4">
        <div class="card-header bg-info text-white d-flex justify-content-between align-items-center rounded-top-4">
         <h4 class="mb-0"><i class="bi bi-person-badge-fill me-2"></i>Admin Listesi</h4>
            <a href="/Admin/AddAdmin/" class="btn btn-light fw-semibold">
                <i class="bi bi-plus-circle me-1"></i> Yeni Admin Ekle
            </a>
        </div>

        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-hover align-middle">
                    <thead class="table-light">
                        <tr>
                            <th scope="col" style="width:5%">#</th>
                            <th scope="col" style="width:20%">Başlık</th>
                            <th scope="col" style="width:20%">Tarih</th>
                            <th scope="col" style="width:20%" class="text-center">İşlemler</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            count++;
                            <tr>
                                <td>@count</td>
                                <td>@item.UserName</td>
                                <td>@item.Password</td>
                                <td class="text-center">
                                    <a href="/Admin/EditAdmin/@item.AdminId" class="btn btn-outline-primary btn-sm me-2">
                                        <i class="bi bi-pencil-square"></i> Güncelle
                                    </a>
                                    <a href="/Admin/DeleteAdmin/@item.AdminId" class="btn btn-outline-danger btn-sm">
                                        <i class="bi bi-trash3"></i> Sil
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>




